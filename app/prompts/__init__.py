"""
Prompts package: versioned prompt templates and builder functions.

Modules:
  templates  — PromptTemplate dataclasses (versioned, configurable)
  builder    — build_support_prompt() — assembles templates with live context
"""
